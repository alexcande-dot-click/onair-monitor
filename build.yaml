targets:
  $default:
    builders:
      drift_dev:
        options:
          # Store DateTime as ISO-8601 text to preserve millisecond precision
          # (notification event times carry millis and are compared exactly).
          store_date_time_values_as_text: true
